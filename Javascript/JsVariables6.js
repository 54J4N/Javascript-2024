let message = "Lets have a coffee";
console.log(message);
//Here output will be the same as previous ,This is because the message written in the code above is a variable.
//To declare a variable we need to used let keyword followed by the var name .
message = "Nice lession!"
console.log(message);